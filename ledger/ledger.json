{
  "project": "npmrevs",
  "idPrefix": "NRV",
  "schemaVersion": "issue.v1",
  "staleAfterDays": 14,
  "description": "cs-npmrevs is a scratch npm registry. It serves npm packages built on this machine, passes every other package through from npmjs.com, and carries package tarballs in container images, one version per image. This ledger tracks its defects, improvements and ideas.",
  "toolVersion": "0.7.0",
  "commitUrlTemplate": "https://github.com/codesweep-ai/npmrevs/commit/{sha}"
}
